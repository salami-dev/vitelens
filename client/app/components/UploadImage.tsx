import React from 'react'

const UploadImage = () => {
  return (
    // hey gpt. create a component using MUI obviously that allows users to upload images, and attempts to upload
    // to a Amazon s3 bucket, and then returns the url of the image
    <div>
      <input type="file" />
      <button>Upload</button>
    </div>
    
  )
}

export default UploadImage